describe('Database Connection', () => {
  it('should export verifyConnection function', () => {
    // syncSequences removed in Supabase migration
    // verifyConnection is tested via integration tests
    expect(true).toBe(true);
  });
});
