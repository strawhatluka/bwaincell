/**
 * @bwaincell/shared
 *
 * Shared types and utilities for Bwaincell monorepo.
 * This package provides type-safe interfaces for backend and frontend communication.
 */

// Database Model Types
export type {
  TaskModel,
  ListModel,
  ListItem,
  ReminderModel,
  ReminderFrequency,
  BudgetModel,
  NoteModel,
  ScheduleModel,
  UserModel,
} from './types/database';

// API types (to be added as needed)
// export type { ... } from './types/api';

// Utility types (to be added as needed)
// export type { ... } from './types/utils';
